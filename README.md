# DataFrame-Virtual-Column
A repository for a simple virtual column creation.

This repository allows for creation of a DataFrame table from two preexisting tables. It handles simple mathematical expressions formed as: 
"argument1 sign argument2". It allows addition, subtraction and multiplication, but doesn't allow division.
